<p id="notice"><%= notice %></p>

<h1>Prestamos</h1>

<table class="table table-striped">
  <thead class="">
    <tr>
      <th>Fecha</th>
      <th>Estado</th>
      <th>Usuario</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @prestamos.each do |prestamo| %>
      <tr>
        <td><%= prestamo.fecha %></td>
        <td><%= prestamo.estado %></td>
        <td><%= prestamo.Usuario.nombres + " " + prestamo.Usuario.apellidos %></td>
        <td><%= link_to 'Ver', prestamo, :class => "btn btn-outline-info btn-sme" %></td>
        <td><%= link_to 'Editar', edit_prestamo_path(prestamo), :class => "btn btn-outline-warning btn-sm" %></td>
        <td><%= link_to 'Eliminar', prestamo, method: :delete, data: { confirm: 'Are you sure?' }, :class => "btn btn-outline-danger btn-sm" %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Nuevo Prestamo', new_prestamo_path, :class => "btn btn-outline-success btn-sm" %>
